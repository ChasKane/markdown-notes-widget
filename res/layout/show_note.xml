<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/showNote"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">
    
    <!-- Background color overlay -->
    <ImageView
        android:id="@+id/widgetBgOverlay"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:scaleType="fitXY"
        android:src="@drawable/transparent" />
    
    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:orientation="vertical"
        android:padding="0dp"
        android:clipToPadding="false">
        
        <!-- Optional title at the top -->
        <TextView
            android:id="@+id/widgetTitle"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/obsidian_text_secondary"
            android:textSize="12sp"
            android:paddingLeft="8dp"
            android:paddingRight="8dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:singleLine="true"
            android:ellipsize="end"
            android:visibility="gone" />
        
        <!-- Padding color overlay for ListView area -->
        <FrameLayout
            android:layout_width="fill_parent"
            android:layout_height="0dp"
            android:layout_weight="1">
            
            <ImageView
                android:id="@+id/widgetPaddingOverlay"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent"
                android:scaleType="fitXY"
                android:src="@drawable/transparent" />
            
            <!-- ListView for note content -->
            <ListView
                android:id="@+id/showNoteList"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent"
                android:divider="#00000000"
                android:dividerHeight="0dp"
                android:padding="0dp"
                android:clipToPadding="false"
                android:scrollbars="none"
                android:background="#00000000" />
        </FrameLayout>
    </LinearLayout>
</FrameLayout>
